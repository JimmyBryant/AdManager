<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html;charset=utf-8">
    <title>Admin-修改密码</title>
    <%include ../head%>
  </head>
  <body>
      <%include ../header%>
      <div id="container">
          <div class="main-left">            
            <% include ../left%>
          </div>
          <div class="main-right">
               <form method="post" actin="/admin/addUser" class="form-horizontal required-form">
                  <dl class="user-sign-box">              
                      <dt><strong>修改密码</strong></dt>
                      <dd>

                         <!-- alert message -->
                         <div class="alert">
                            <button type="button" class="close" data-dismiss="alert">×</button>
                            <strong></strong><span class="mes"></span>
                         </div>

                          <div class="control-group">
                              <label class="control-label" for="inputOldPassword">旧密码</label>
                              <div class="controls">
                              <input type="password" id="inputOldPassword" name="oldPassword" placeholder="Password" class="required">
                              </div>
                          </div>

                         <div class="control-group">
                              <label class="control-label" for="inputNewPassword">新密码</label>
                              <div class="controls">
                              <input type="password" id="inputNewPassword" name="newPassword" placeholder="New Password" class="required">
                              </div>
                         </div>

                        <div class="control-group">
                              <label class="control-label" for="inputSecondPassword">重复新密码</label>
                              <div class="controls">
                              <input type="password" id="inputSecondPassword" name="secondPassword" placeholder="New Password Again" class="required">
                              </div>
                         </div>

                        <div class="control-group">
                              <div class="controls">
                                <input type="submit" class="btn btn-success btn-medium" value="确定修改"/>
                              </div>
                         </div>
                      </dd>
                  </dl>
               </form>
          </div>

      </div>
      <%include ../script%>
      <script type="text/javascript" >
        seajs.use('/js/validate',function  (validate) {
          validate.validateChangePsd();
        });
      </script>
      <%include ../footer%>
 </body>
 </html>